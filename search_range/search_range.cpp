#include <iostream>
#include <array>
#include <vector>

using std::vector;

vector<int> searchRange(const vector<int> &nums, int target) {
    return
}

int main() {
    auto r = searchRange(vector<int>{1, 2, 3, 3, 3, 4}, 3);
    std::cout << r[0] << ' ' << r[1] << std::endl;
    return 0;
}
